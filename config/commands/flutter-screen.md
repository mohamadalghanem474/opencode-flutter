---
description: Create or update a Flutter screen using the repo's existing routing, UI, and state patterns
agent: build
model: opencode/gemini-3.1-pro
---

Implement the Flutter screen request: $ARGUMENTS

Requirements:

- Inspect the repo's routing, theming, screen composition, and state patterns first.
- Create or update the screen, supporting widgets, and wiring with minimal churn.
- Handle loading, empty, error, and success states where relevant.
- Keep the work aligned with existing architecture and package choices.
- Add or update focused tests when behavior changes.
- Summarize touched files, validation run, and any follow-up steps.
