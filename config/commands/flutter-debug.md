---
description: Investigate a Flutter bug across UI, state, data, platform, and validation layers
agent: general
model: opencode/gemini-3-pro
---

Debug this Flutter issue: $ARGUMENTS

Requirements:

- Map the failing flow before editing.
- Trace the issue across UI, state, repositories, services, navigation, and platform setup as relevant.
- Fix the root cause rather than applying a surface patch when practical.
- Add or update focused validation if behavior changes.
- Report likely cause, changes made, and residual risk.
